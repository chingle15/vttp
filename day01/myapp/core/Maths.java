package myapp.core;

import java.io.Console;

public class Maths;

public static void main (Strings[] args) {
     Console cons = system.console();
     String input_1 = cons.readLine("Operand 1: ");
     Integer operand = Integer.parseInt(input_1)
     if (input_1 > 0) {
         System.out.printf("Operand 1: %s\n,input_1")
     } else {
         system.err.println("You have not entered a number");
     }

     Console cons = system.console();
     String input_2 = cons.readLine("Operand 2: ");
     Integer operand = Integer.parseInt(input_2)
     if (input_2 > 0) {
         System.out.printf("Operand 2: %s\n,input_2")
     } else {
         system.err.println("You have not entered a number");
     }
     Console cons = system.console();
     String operator = cons.readLine("Choose an operator : (add,div,sub,mul");
    
     if (operator = add)
     input_1 + input_2 = input_3 {
         System.out.printf("input_1 %s input_2 = input_3")
     } else {
         system.err.println("You have not chosen an operator");
         if (operator = div) 
         input_1 / input_2 = input_3{
            System.out.printf("input_1 %s input_2 = input_3")
        } else {
            system.err.println("You have not chosen an operator");
            if (operator = mul)
            input_1*input_2 = input_3{
                System.out.printf("input_1 %s input_2 = input_3")
            } else {
                system.err.println("You have not chosen an operator");
                if (operator = sub)
                input_1 - input_2 = input_3 {
                    System.out.printf("input_1 %s input_2 = input_3")
                } else {
                    system.err.println("You have not chosen an operator");
                    


     }

}